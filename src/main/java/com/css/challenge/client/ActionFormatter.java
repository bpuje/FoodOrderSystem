package com.css.challenge.client;import com.fasterxml.jackson.databind.ObjectMapper;import com.fasterxml.jackson.databind.node.ArrayNode;import com.fasterxml.jackson.databind.node.ObjectNode;import java.util.List;public class ActionFormatter {    private static final ObjectMapper objectMapper = new ObjectMapper();    public static String formatActions(List<Action> actions) {        ArrayNode actionsArray = objectMapper.createArrayNode();        for (Action action : actions) {            ObjectNode actionNode = objectMapper.createObjectNode();            actionNode.put("timestamp", action.getTimestamp()); // Ensure timestamp is in microseconds            actionNode.put("id", action.getId());            actionNode.put("action", action.getAction());            actionsArray.add(actionNode);        }        return actionsArray.toPrettyString();    }}